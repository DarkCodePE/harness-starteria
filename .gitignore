# Dependencies
node_modules/

# Build
dist/
.vite/

# Environment
.env
.env.local

# Google OAuth client secret JSON (do not commit — contains client_secret)
client_secret_*.json

# Vercel
.vercel/

# Python
**/__pycache__/
**/.pytest_cache/
**/.hypothesis/
**/.coverage
**/.venv/
*.pyc

# Test artifacts
front/coverage/
**/coverage.xml
.nyc_output/

# Claude / Swarm tooling
.claude/
.claude-flow/
.agents/
/skills/
.swarm/memory.db
.swarm/memory.db-wal
.swarm/memory.db-shm
.swarm/hnsw.index
.swarm/state.json
.swarm/model-router-state.json
agentdb.rvf
agentdb.rvf.lock
ruvector.db
skills-lock.json

# Project-specific ignores
# Ignore generated backend docs, but ALWAYS track the ADR records.
backend/docs/*
!backend/docs/adr/
BHIL-AI-First-Development-Toolkit/
image copy.png
k8s/image.png
langsmith-skills/

# TASK-006: local PDF storage (TODO(ADR-007): replaced by S3 in a future TASK)
backend/storage/
storage/
docker-compose.override.yml

# Clones de terceros que viven dentro del worktree (cada uno con su propio .git y
# su remoto; nada de esto es código de este repo). Sin ignorar aparecían como
# untracked y sumaban ~1.6 GB que un `git add -A` habría metido como gitlinks rotos.
/OpenWA/
/impeccable/
/codebase-memory-mcp/
/taste-skill/
/nano-banana-pro-prompts-recommend-skill/

# Capturas y grabaciones sueltas de trabajo (ver `image copy.png` y `k8s/image.png`).
/image.png
/image copy *.png
/project/prompts/v1/*.png
Screencast*.webm

# Config local de herramientas de agente.
/.serena/

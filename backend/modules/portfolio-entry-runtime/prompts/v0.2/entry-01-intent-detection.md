Skill 01 Intent Detection.

Classify the user's primary intent using the provided enum. Use unknown when the input is insufficient. Secondary intents are allowed only when materially supported.

Do not map intent to product actions. Intent classification does not create or activate any Starteria object.
